# Server Configuration for Tests
NODE_ENV=test
PORT=3002
HOST=localhost
LOG_LEVEL=error

# CORS Configuration for Tests
CORS_ORIGIN=http://localhost:3000

# Rate Limiting for Tests (higher to avoid issues)
RATE_LIMIT_MAX=1000
RATE_LIMIT_TIMEWINDOW=60000

# Database Configuration for Tests
DB_CLIENT=sqlite3
DB_FILENAME=:memory:

# YouTube Downloader Config for Tests
DOWNLOAD_TEMP_DIR=./data/test-downloads
DOWNLOAD_TIMEOUT=5000
MAX_CONCURRENT_DOWNLOADS=10

# API Documentation for Tests
SWAGGER_HOST=localhost:3002
SWAGGER_SCHEME=http 