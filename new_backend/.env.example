# Example environment variables for YouTube Downloader API

# API configuration
API_V1_STR=/api/v1
PROJECT_NAME="YouTube Downloader API"

# Download settings
DOWNLOAD_PATH=/tmp/youtube_downloads
FILE_EXPIRY_SECONDS=300
MAX_RESOLUTION=1080p

# CORS settings (comma-separated URLs)
BACKEND_CORS_ORIGINS=http://localhost,http://localhost:3000,http://localhost:5173,http://localhost:5174

# Debug mode (set to 'true' for development)
DEBUG=false

# Optional: Custom FFmpeg path (leave empty to use system default)
FFMPEG_PATH= 